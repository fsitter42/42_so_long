	MAPSTUFF.c notes darunter beachten
	MAPSTUFF.c notes darunter beachten
	MAPSTUFF.c notes darunter beachten


int check_path(struct mapdata, mapfile)function > mapcopy = Malloc/calloc(sizeof(map(+1?)); int i =0; while(mapcopy){ strdup(mapcopy[i], map[i]; i++;} 
mapcopy[i] = NULL; fill(mapdata, mapcopy);


Map:


	check for valid path > floodfill
	after valid path > check with strchr if collectables are left


Winwdow:

Main:
	DONE! check check_input
	DONE! open
	DONE! fill struct
	DONE! check and find stuff
	check path
	DONE! free map function
	delete ft_printf int free_map

	check validity with against struct
	maybe create a stuct called valid to safe valid values and compare against them


