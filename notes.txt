Winwdow:
	create_game(t_mapdata, t_imagedata); 
	-> mlx init() ? 0 > clear_all : load_pixmaps();
	mlx_new_windows
	fillmap
	hookinit
	mkxloop
	return -> clear all
	
	

Main:
	DONE! check check_input
	DONE! open
	DONE! fill struct
	DONE! check and find stuff
	DONE! check path
	DONE! free map function
	DONE! delete ft_printf int free_map


create map() give map and windowdata to it
	declare t_imagedata[5], widgh and height -> with values * pixels
	set windata->map to mapdata ->img to img
				->win_ptr to NULL, -> mlxptr to mlx_init();
									if it fails clear everything with CLEARFUNCTION
	load pixmaps(windata);
	data->winptr to mlx_new_windows(mlx_ptr, w, h, "name")
		if it fails clear all things
	fillmap with pixmaps
	initialize hook
	if mlxloop
		clear all
	clear all
	return


	game.c zeile 98 zeile zu lange
	geht es mit formatting oder funktion
	zeile 42
